platform: linux

image_resource:
  type: docker-image

inputs:
- name: gpbackup_src
- name: ccp_src
- name: cluster_env_files
- name: dummy_seclabel
  optional: true

params:
  default_ami_user: centos

run:
  path: gpbackup_src/ci/scripts/setup-centos-env.bash
