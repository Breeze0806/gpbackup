platform: linux

image_resource:
  type: registry-image
  source:
    repository: gcr.io/data-gpdb-public-images/ccp
    tag: latest

inputs:
- name: gpbackup_src
- name: ccp_src
- name: cluster_env_files
- name: dummy_seclabel
  optional: true

params:
  USER:

run:
  path: gpbackup_src/ci/scripts/setup-centos-env.bash
